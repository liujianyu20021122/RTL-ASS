interface:
  display_name: "RTL-ASS"
  short_description: "Audited RTL knowledge retrieval for Codex"
  default_prompt: "Use $rtl-ass to retrieve calibrated RTL references, then help Codex complete this RTL task while preserving any selected or project-local verification flow."
policy:
  allow_implicit_invocation: true
